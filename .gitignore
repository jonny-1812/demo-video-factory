node_modules/
out/
.env
.env.*

# NOTE: src/generated/ is committed with a placeholder so the project compiles on a
# fresh clone (Root.tsx imports it). `/demo` regenerates it per run — don't commit
# those local changes.

# Large media files
*.m4a
*.wav
public/broll*.mp4
public/music_*_raw.*
public/vo*.mp3

# OS
.DS_Store

# Build artifacts
dist/
*.tsbuildinfo

# Temp
/tmp/demo_*.txt

# Per-run scraped assets (regenerated by agent/scan.ts)
public/real/
